{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "install",
            "command": "rvm use ruby-2.5.8 && rvm gemset use tshield-vcr-via-cep && bundle install --binstubs"
        }
    ]
}